PORT=5000
# switch to your local MongoDB when developing/testing
# e.g. mongodb://localhost:27017/doctors-cares
MONGO_URI=mongodb://localhost:27017/doctors-cares

# a remote connection string (Atlas) can be used in production, but
# the code will fall back to localhost if the remote host is unreachable.
# MONGO_URI=mongodb+srv://user:pass@cluster0.example.mongodb.net/doctorscares?retryWrites=true&w=majority

JWT_SECRET=DoctorsCares_SuperSecret_JWT_Key_2024_Change_This_In_Production
